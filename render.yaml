services:
  - type: web 
    name: testCloudApp
    runtime: node
    repo: https://github.com/Celina-cbn/cloudTest.git
    buildCommand: npm install
    startCommand: npm start
    region: frankfurt
    plan: free
    
  databases:
    - name: db-name
      plan: free
      region: frankfurt
